{
  "name": "helix",
  "version": "1.0.0",
  "main": "postcss.config.js",
  "scripts": {
    "build": "cp src/index.html public/index.html; postcss src/styles.css -o public/styles.css",
    "build2": "./build.sh",
    "start": "browser-sync start --server 'public' --files 'public src' --middleware 'npm run build'"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "autoprefixer": "^10.4.14",
    "postcss": "^8.4.22",
    "tailwindcss": "^3.3.1"
  },
  "description": "",
  "devDependencies": {
    "browser-sync": "^2.29.1",
    "postcss-cli": "^10.1.0",
    "postcss-import": "^15.1.0"
  }
}
